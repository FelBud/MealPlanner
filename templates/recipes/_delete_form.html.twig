<form method="post" action="{{ path('app_recipes_delete', {'id': recipe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recipe.id) }}">
    <div class="text-center"><button class="btn btn-outline-dark margin-left:10px btnsize mt-2"><i class="fa-regular fa-circle-xmark me-2"></i>Delete</button></div>
</form>