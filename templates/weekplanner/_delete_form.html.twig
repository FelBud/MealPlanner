<form method="post" action="{{ path('app_weekplanner_delete', {'id': weekplanner.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ weekplanner.id) }}">
    <button class="btn">Delete</button>
</form>
